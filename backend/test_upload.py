import requests

# 1. Login to get token
login_data = {
    "username": "admin",
    "password": "password"  # Replace with actual password if needed, or check how to bypass
}
# But I can just add a mock session to the store directly in the backend code for testing
